# 项目总结：基于微分Koopman MPC的质量-弹簧-阻尼系统控制

## 1. 项目概述
本项目是对 GitHub 项目 [JuliaDifferentialMPC](https://github.com/marcowus/JuliaDifferentialMPC) 的 Python 重构与完整实现。项目旨在展示一种在线微分 Koopman 模型预测控制（MPC）算法，用于非线性或未知的动力学系统控制。本实现针对质量-弹簧-阻尼（Mass-Spring-Damper, MSD）系统，验证了该方法在在线学习、抗干扰和轨迹跟踪方面的有效性。

## 2. 方法论
本项目采用了以下核心技术：
*   **离线 Koopman 建模 (EDMD)**: 利用生成的 PRBS (伪随机二进制序列) 激励数据，通过扩展动态模式分解 (EDMD) 算法，学习系统的初始线性提升模型。提升函数采用高达二阶的多项式特征。
*   **在线微分学习**: 在闭环控制过程中，利用 PyTorch 的自动微分功能，实时更新 Koopman 算子矩阵 ($A, B, C$) 以及控制计划 ($U_{mpc}$)。
*   **模型预测控制 (MPC)**: 结合在线修正的模型，通过梯度下降直接优化控制输入序列，以最小化跟踪误差和控制代价。

## 3. 代码结构
项目代码位于 `python_differential_mpc/` 目录下，主要包含以下模块：

*   `msd.py`: **系统动力学**。实现了质量-弹簧-阻尼系统的连续和离散时间仿真 (RK4 积分)。
*   `generate_data.py`: **数据生成**。模拟系统在 PRBS 激励下的响应，生成 `X.csv` (状态) 和 `U.csv` (输入) 用于离线训练。
*   `offline_koopman.py`: **离线学习**。实现多项式 EDMD 算法，拟合初始 Koopman 矩阵 $A_0, B_0, C_0$。
*   `online_mpc.py`: **在线控制器**。定义了 `DifferentialKoopmanMPC` 类 (PyTorch Module)，包含可训练的微分修正项 $dA, dB, dC$ 和控制序列 $U_{mpc}$，以及联合损失函数和优化步。
*   `main.py`: **主程序**。集成数据生成、模型训练和闭环仿真流程。并在 $t=15s$ 施加外部扰动以测试鲁棒性。
*   `visualize.py`: **可视化**。生成符合学术论文标准的实验结果图表。

## 4. 实验结果
### 实验 1: 基础稳定性测试 (30s)
仿真时长为 30 秒，步长 $dt=0.01s$。在 $t=15s$ 时施加了速度突变扰动。
*   **状态跟踪**: 控制器能够迅速将系统状态调节至原点。面对 $t=15s$ 的剧烈扰动，系统表现出良好的鲁棒性，并在短时间内恢复稳定。

### 实验 2: 长时间恢复测试 (60s)
为了验证系统在大幅扰动后的长期收敛性，将仿真时间延长至 60 秒。
*   **收敛时间**: 在 $t=15s$ 施加扰动后，系统在大约 13.86 秒后（即 $t \approx 28.86s$ 时）重新进入稳定区域（状态绝对值小于 0.05）。
*   **长期稳定性**: 在 $t=30s$ 至 $t=60s$ 的区间内，系统保持在极小的误差范围内运行，证明在线学习算法没有引入发散的不稳定性。

## 5. 可视化结果
结果图片保存在 `python_differential_mpc/results/` 目录下：

1.  **System State Response (`state_tracking.png`)**: 展示了位置 ($p$) 和速度 ($v$) 随时间的变化，清晰可见扰动后的恢复过程。
2.  **Control Input (`inputs.png`)**: 展示了控制力 ($u$) 的变化曲线。
3.  **Computation Time (`computation_time.png`)**: 展示了在线优化步骤的耗时情况。

## 6. 结论
通过 Python 重构，本项目成功复现了原 Julia 项目的核心功能。利用 PyTorch 进行在线微分学习和 MPC 优化被证明是处理此类控制问题的有效手段，尤其是在需要自适应模型误差和外部干扰的场景下。
